<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://utf7.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://utf7.github.io/" rel="alternate" type="text/html" /><updated>2026-08-24T09:30:20+08:00</updated><id>https://utf7.github.io/feed.xml</id><title type="html">utf7</title><subtitle>utf7（陈叶超）的个人博客，记录 AI Agent、AI Data Infra、云原生与大数据工程实践。</subtitle><author><name>utf7 / 陈叶超</name></author><entry><title type="html">DeepSeek Harness 终于来了并重磅开源：模型之外，还有更重要的一层</title><link href="https://utf7.github.io/2026/08/13/deepseek-harness/" rel="alternate" type="text/html" title="DeepSeek Harness 终于来了并重磅开源：模型之外，还有更重要的一层" /><published>2026-08-13T00:00:00+08:00</published><updated>2026-08-13T00:00:00+08:00</updated><id>https://utf7.github.io/2026/08/13/deepseek-harness</id><content type="html" xml:base="https://utf7.github.io/2026/08/13/deepseek-harness/"><![CDATA[<p>就在刚刚，DeepSeek Harness 重磅发布。超哥没注意点开 DeepSeek 的 GitHub，本想去看看 DeepSeek V4 Pro 相关的文档，结果意外发现 DeepSeek Harness 已经在仓库里开源了。</p>

<p>对关注 DeepSeek Agent 方向的人来说，这次发布的分量不轻。它不是新模型，也不是给 DeepSeek API 套一个聊天界面，而是把模型放进真实工作环境的那套运行系统。</p>

<p>简单说，<strong>模型负责思考，Harness 负责让它干活。</strong></p>

<h2 id="harness-到底是什么">Harness 到底是什么？</h2>

<p>关于 DeepSeek Harness，前几天在我的视频号聊过。有兴趣可以听一听：</p>

<p><a href="https://weixin.qq.com/sph/A71bgxIgl6">https://weixin.qq.com/sph/A71bgxIgl6</a></p>

<p>直接调用模型并不难。难的是模型决定「下一步要做什么」之后，系统如何接住它。</p>

<p>模型想读文件，谁来检查路径？想运行命令，在哪个环境执行？操作有风险，是直接放行还是先让用户确认？任务跑到一半中断，下次能不能接着做？</p>

<p>这些都不是 Prompt 能解决的问题。</p>

<p>DeepSeek Harness 做的，就是把模型、工具、文件系统、终端、会话记录、沙箱和权限控制接到一起，组成一套完整的 Agent Runtime。</p>

<p>它提供 Web UI，也支持 Headless 模式和 Python SDK，可以独立使用，也能嵌入评测平台、CI 流程或企业内部系统。</p>

<h2 id="从算法与-infra-co-design到模型与-harness-co-design">从算法与 Infra Co-Design，到模型与 Harness Co-Design</h2>

<p>我一直有个观点：AI 的进步从来不只靠算法。</p>

<p>大模型训练阶段，算法与 Infra 一直在共同演进。新算法会带来新的计算、通信和显存需求，基础设施的能力也会反过来决定哪些算法真正跑得起来。</p>

<p>Agent 时代也是同样的道理，只是关系变成了 Model 与 Harness 的 Co-Design。模型为什么进度这么快，个人感觉和 Agent Skill 的发明以及 Harness 协同设计有巨大的关系。</p>

<p>模型需要 Harness 提供上下文、工具和执行环境；Harness 也需要根据模型的真实表现调整工具定义、上下文组织、重试机制和权限策略。</p>

<p>两边不能各做各的。</p>

<p>模型工具调用不准，问题可能不在模型，也可能是 Tool Schema 写得不清楚。任务执行到一半跑偏，可能是规划能力不足，也可能是 Harness 返回的信息不适合模型理解。</p>

<p>只有把模型放进真实任务里跑，完整记录中间过程，才能知道问题究竟出在哪里。</p>

<h2 id="一套系统两个优化方向">一套系统，两个优化方向</h2>

<p>DeepSeek Harness 使用追加式事件日志记录用户消息、模型输出、工具调用和执行结果。这些记录不仅可以用来恢复和回放会话，也为后续评测和数据分析留下了基础。</p>

<p>从这个角度看，模型与 Harness 可以形成下面的协同关系：</p>

<p><img src="/images/posts/ai/deepseek-harness/model-harness-codesign.png" alt="模型与 Harness 协同优化" /></p>

<p>一边是模型优化，例如规划、工具选择、参数生成和错误恢复。</p>

<p>另一边是 Harness 优化，例如 Prompt 组装、工具协议、上下文管理、模型路由、重试逻辑和安全策略。</p>

<p>真实任务产生执行轨迹，轨迹经过清洗和评测，再分别反馈给模型与 Harness。系统变好以后，可以处理更多任务，进而产生覆盖面更广的数据。</p>

<p>这才是数据飞轮：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>真实任务 → 执行轨迹 → 失败归因 → 模型与 Harness 同时改进 → 更高的任务完成率 → 更多真实任务
</code></pre></div></div>

<p>需要说明的是，DeepSeek Harness 目前并没有宣称会自动把会话数据用于训练。它提供的是事件日志、持久化、回放和遥测等基础能力。训练回流、数据脱敏和用户授权仍然需要团队自己完成。</p>

<h2 id="话不多说把代码拉下来跑跑看">话不多说，把代码拉下来跑跑看</h2>

<p>内测声明还没有去掉：</p>

<p><img src="/images/posts/ai/deepseek-harness/preview-notice.png" alt="DeepSeek Harness 内测声明" /></p>

<p>首页可以添加本地项目，有好几种模式：标准模式、PTC 模式、极简模式、创造模式。</p>

<p><img src="/images/posts/ai/deepseek-harness/homepage-modes.png" alt="DeepSeek Harness 首页与运行模式" /></p>

<p>看来 DeepSeek 还是很在乎 Trace 链路的。这点和我们开发的喜马 DataClaw 智能体不谋而合，不能说有点类似，几乎是一模一样！哈哈哈😊，特别是倒数第二个红色框起来的。</p>

<p>让它了解一下 DataClaw：</p>

<p><img src="/images/posts/ai/deepseek-harness/chat-dataclaw.png" alt="向 DeepSeek Harness 介绍 DataClaw" /></p>

<p><img src="/images/posts/ai/deepseek-harness/dataclaw-intro.png" alt="DataClaw 介绍回复" /></p>

<p><img src="/images/posts/ai/deepseek-harness/architecture-review.png" alt="DeepSeek Harness 对 DataClaw 架构的评价" /></p>

<p>操，情绪价值拉满啊：</p>

<blockquote>
  <p>设计者的架构品味和踩坑经验都体现在代码里，这是一个「有经验的人带着 AI 辅助高速迭代」的产物；但它是靠个人纪律在运转的——一旦核心维护者离开或 AI 提交失控，没有 CI 和 lint 门禁会让债务快速恶化。</p>
</blockquote>

<p>继续继续。</p>

<p>通用设置：</p>

<p><img src="/images/posts/ai/deepseek-harness/general-settings.png" alt="通用设置" /></p>

<p>模型设置：</p>

<p><img src="/images/posts/ai/deepseek-harness/model-settings.png" alt="模型设置" /></p>

<p>插件设置：</p>

<p><img src="/images/posts/ai/deepseek-harness/plugin-settings.png" alt="插件设置" /></p>

<p>Agent 预设：</p>

<p><img src="/images/posts/ai/deepseek-harness/agent-preset.png" alt="Agent 预设" /></p>

<h2 id="一切皆插件">「一切皆插件」</h2>

<p>DeepSeek Harness 基于 Cordis 构建，采用「一切皆插件」的架构。</p>

<h3 id="架构先看全貌">架构先看全貌</h3>

<p>我把仓库和架构文档过了一遍。DeepSeek Harness 最鲜明的地方不是 Web UI，而是它把整个运行系统拆成了一棵 Cordis 插件树。</p>

<p><img src="/images/posts/ai/deepseek-harness/cordis-plugin-tree.png" alt="DeepSeek Harness 技术架构" /></p>

<p>模型适配器是插件，工具系统是插件，会话日志是插件，Agent Loop 本身也是插件。开发者可以在不修改整套框架的情况下，更换模型，为自己的场景配置插件和权限。</p>

<p>这套设计很适合做对比实验。</p>

<p>固定 Harness 更换模型，可以比较不同模型的任务完成率；固定模型调整工具和上下文，可以判断系统层改动是否有效。只有能够控制变量，Co-Design 才有实际意义。</p>

<h2 id="prompt-不是安全边界">Prompt 不是安全边界</h2>

<p>DeepSeek Harness 另一个比较务实的地方，是没有把安全全部交给模型。</p>

<p>模型发起工具调用后，请求还要经过参数检查、权限策略、用户审批和沙箱，才能真正访问文件或执行命令。</p>

<p>这比在系统提示词里写一句「不要执行危险操作」可靠得多。</p>

<p>模型可以犯错，权限系统不能跟着碰运气。</p>

<h2 id="如何运行">如何运行？</h2>

<p>安装 Node.js 后，可以直接启动 Web UI：</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npx @deepseek-ai/dsh web
</code></pre></div></div>

<p>默认地址为：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://127.0.0.1:3080
</code></pre></div></div>

<p>配置模型并选择工作目录后，就可以创建任务。</p>

<p>项目也提供 Python SDK：</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> pip <span class="nb">install </span>deepseek-harness-sdk
</code></pre></div></div>

<p>模型方面并不只支持 DeepSeek，也可以配置 OpenAI、Anthropic、自托管服务和其他兼容接口。详见 <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/user/guide/providers.zh.md">模型配置文档</a>。</p>

<h2 id="现在还很早">现在还很早</h2>

<p>DeepSeek Harness 目前仍处于 Developer Preview，官方明确提醒后续会有破坏兼容性的修改。</p>

<p>它也不是一款开箱即用的普通 AI 编程助手。Cordis、Profile、Bundle 和插件配置都有学习成本。现阶段更适合正在搭建 Agent 平台、设计受控执行环境，或者研究模型与工具协同的团队。</p>

<p>但方向已经很清楚。</p>

<p>过去我们更多关注模型本身。到了 Agent 阶段，模型能力只是系统的一部分。真正决定 Agent 能不能进入生产环境的，是模型、Harness 和 Infra 能否一起设计、一起迭代。</p>

<p>模型产生行动，Harness 执行动作并记录结果，数据再推动两边继续改进。</p>

<p>这可能才是 DeepSeek Harness 最值得期待的地方。</p>

<p>项目地址：<a href="https://github.com/deepseek-ai/deepseek-harness">deepseek-ai/deepseek-harness</a></p>

<p>PS：第一时间让 DeepWiki 索引了 DeepSeek Harness 代码库：</p>

<p><a href="https://deepwiki.com/deepseek-ai/deepseek-harness">https://deepwiki.com/deepseek-ai/deepseek-harness</a></p>

<hr />

<p>本文同步自公众号「程序员乌托邦」：<a href="https://mp.weixin.qq.com/s/wXRoFXFeW0Q9QSGOeRx1TQ">DeepSeek Harness 终于来了并重磅开源：模型之外，还有更重要的一层</a></p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="AI Agent" /><summary type="html"><![CDATA[模型负责思考，Harness 负责让它干活。DeepSeek Harness 开源后，模型与 Harness 的 Co-Design 才是更值得关注的一层。]]></summary></entry><entry><title type="html">2023 H2 生活 OKR</title><link href="https://utf7.github.io/2023/09/03/my-life-okr-for-2023H2/" rel="alternate" type="text/html" title="2023 H2 生活 OKR" /><published>2023-09-03T00:00:00+08:00</published><updated>2023-09-03T00:00:00+08:00</updated><id>https://utf7.github.io/2023/09/03/my-life-okr-for-2023H2</id><content type="html" xml:base="https://utf7.github.io/2023/09/03/my-life-okr-for-2023H2/"><![CDATA[<h1 id="2023-h2-生活-okr">2023 H2 生活 OKR</h1>

<h2 id="o1-健康管理升级通过引入良好的健康管理机制并在日常生活中落地以改善睡眠质量增强免疫力保持精力延缓衰老提高观感使得更加健康活力">O1 【健康管理升级】通过引入良好的健康管理机制并在日常生活中落地，以改善睡眠质量，增强免疫力、保持精力，延缓衰老，提高观感，使得更加健康活力。</h2>

<h3 id="kr1------通过健身运动以及控制饮食等手段降低脂肪含量将体重从125下降到120降低4-降低关节心血管心脏等健康风险-为长期可持续健康发展打下坚实基础同时提供更好的观感使得看起来更年轻">KR1      通过健身、运动以及控制饮食等手段，降低脂肪含量，将体重从125下降到120，降低4% ,降低关节、心血管、心脏等健康风险， 为长期可持续健康发展打下坚实基础，同时提供更好的观感，使得看起来更年轻。</h3>

<h3 id="kr2------通过引入早睡晚起中午午休星期天补觉以及少看手机等策略来改善睡眠质量以缓解疲劳增强免疫力提高大脑活力保持精力增强免疫力以及延缓衰老为更好的生活和工作打下坚实的基础">KR2      通过引入早睡晚起、中午午休、星期天补觉以及少看手机等策略，来改善睡眠质量，以缓解疲劳，增强免疫力，提高大脑活力，保持精力，增强免疫力以及延缓衰老，为更好的生活和工作打下坚实的基础。</h3>

<h3 id="kr3------通过撸铁游泳散步以及进食高蛋白食品减少糖分饮料咖啡因摄入提高肌肉含量提高不限于胸大肌腹肌三角肌斜方肌背阔肌的维度和颗粒度使其更加有锐度和颗粒感同时提高肺活量和改善心血管健康增强免疫力改善心情使得身体更健康">KR3      通过撸铁、游泳、散步以及进食高蛋白食品、减少糖分、饮料、咖啡因摄入，提高肌肉含量，提高不限于胸大肌、腹肌、三角肌、斜方肌、背阔肌的维度和颗粒度，使其更加有锐度和颗粒感，同时提高肺活量和改善心血管健康，增强免疫力，改善心情，使得身体更健康。</h3>

<h2 id="o2------家庭管理升级响应号召积极生娃改善家庭关系关注小孩学习成长提高幸福感">O2      【家庭管理升级】响应号召，积极生娃；改善家庭关系，关注小孩学习成长，提高幸福感。</h2>

<h3 id="kr1------投入更多时间在小孩陪伴上一块陪伴其学习生活以及健康快乐成长">KR1      投入更多时间在小孩陪伴上，一块陪伴其学习、生活以及健康快乐成长。</h3>

<h3 id="kr2------通过积极响应国家二胎号召为国家以及人类的可持续发展做贡献积极生娃丰富家庭生活增加家庭成员之间的互动和交流缓解独生子女的压力避免感到孤独提高娃的协作精神">KR2      通过积极响应国家二胎号召，为国家以及人类的可持续发展做贡献，积极生娃，丰富家庭生活，增加家庭成员之间的互动和交流，缓解独生子女的压力，避免感到孤独，提高娃的协作精神。</h3>

<h3 id="kr3------每月至少做4次饭4次市内或者周边出行游玩完成一次旅游出行">KR3      每月至少做4次饭，4次市内或者周边出行游玩，完成一次旅游出行。</h3>

<h2 id="o3---生活管理升级通过完成装修夯实家居基础环境为家人提供良好的生活和学习环境改善财务状况">O3   【生活管理升级】通过完成装修，夯实家居基础环境，为家人提供良好的生活和学习环境；改善财务状况。</h2>

<h3 id="kr1------引入水电木瓦油等工艺高质量完成硬装工作满足水电燃气保暖制冷安全防护等基本需求">KR1      引入水电木瓦油等工艺，高质量完成硬装工作，满足水、电、燃气、保暖、制冷、安全防护等基本需求。</h3>

<h3 id="kr2------通过引入全屋定制淋浴房马桶花洒等来满足基本日常储物洗簌等居住诉求">KR2      通过引入全屋定制、淋浴房、马桶、花洒等来满足基本日常储物、洗簌等居住诉求。</h3>

<h3 id="kr3------通过购买家具家电以及引入软装搭配来满足日常家庭居住休闲娱乐诉求同时改善家居观感">KR3      通过购买家具、家电以及引入软装搭配来满足日常家庭居住、休闲、娱乐诉求，同时改善家居观感。</h3>

<h3 id="kr4----通过开源节流主要手段是节流改善家庭入不敷出的财务状况提高家庭抗风险能力和长期可持续发展">KR4    通过开源节流（主要手段是节流），改善家庭入不敷出的财务状况，提高家庭抗风险能力和长期可持续发展。</h3>

<h2 id="o4-无用技能升级调研至少1个无用技能比如乐器拳击摩托车等考虑学习掌握一个无用技能">O4 【无用技能升级】调研至少1个无用技能，比如乐器、拳击、摩托车等，考虑学习掌握一个无用技能。</h2>

<h3 id="kr1----调研乐器口琴吉他等拳击等无用技能丰富日常生活">KR1    调研乐器（口琴、吉他等）、拳击等无用技能，丰富日常生活。</h3>]]></content><author><name>utf7 / 陈叶超</name></author><category term="生活" /><summary type="html"><![CDATA[2023 H2 生活 OKR]]></summary></entry><entry><title type="html">大数据资源使用效率统计</title><link href="https://utf7.github.io/2022/07/20/spark-resource-used-metrics/" rel="alternate" type="text/html" title="大数据资源使用效率统计" /><published>2022-07-20T00:00:00+08:00</published><updated>2022-07-20T00:00:00+08:00</updated><id>https://utf7.github.io/2022/07/20/spark-resource-used-metrics</id><content type="html" xml:base="https://utf7.github.io/2022/07/20/spark-resource-used-metrics/"><![CDATA[<p>大数据资源使用情况统计：</p>

<p>物理内存使用率：
物理CPU使用率：</p>

<p>物理 CPU使用率（白天/晚上）：
物理 内存使用率（白天/晚上）：</p>

<p>YARN CPU使用率：
YARN 内存使用率：</p>

<p>YARN CPU使用率（白天/晚上）：
YARN 内存使用率（白天/晚上）：</p>

<p>Spark 相关：<br />
每秒 Container 数量<br />
每天 Spark 任务数量. 
平均每个 Spark 任务 executor 数量. 
平均每个 Spark Executor 申请的堆内存(spark.executor.memory). 
平均每个 Spark Executor 申请的堆外内存（spark.executor.memoryoverhead). 
平均每个 Spark Executor 申请的 Cores 数量(spark.executor.cores). 
平均每个 Spark Executor Core 对应的内存比例(spark.executor.memory=8G,spark.executor.cores=4，则为2). 
平均每个 Spark Executor 申请的总内存（spark.executor.memory+spark.executor.memoryoverhead）. 
平均 Spark Executor 堆内存使用率 sum(executor.max_heap_used<em>executorNum)/sum(spark.executor.memory</em>executorNum) 注意算法. 
平均 Spark Executor 堆外内存使用率 sum(executor.max_offheap_used<em>executorNum)/sum(spark.executor.memoryoverhead</em>executorNum) 注意算法. 
平均 Spark Executor 总内存使用率   sum((executor.max_offheap_used+executor.max_heap_used)<em>executorNum)/sum((spark.executor.memory+spark.executor.memoryoverhead)</em>executorNum) 注意算法
(不太严谨，堆内外的峰值内存不一定同时出现）.</p>

<p>平均每个Spark 任务的 Task 数量. 
平均每个Spark shuffle 读的数据量. 
平均每个Spark shuffle 写的数据量. 
。。。。</p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="大数据" /><summary type="html"><![CDATA[大数据资源使用效率统计]]></summary></entry><entry><title type="html">程序员书写用词规范</title><link href="https://utf7.github.io/2022/06/12/word-specification/" rel="alternate" type="text/html" title="程序员书写用词规范" /><published>2022-06-12T00:00:00+08:00</published><updated>2022-06-12T00:00:00+08:00</updated><id>https://utf7.github.io/2022/06/12/word-specification</id><content type="html" xml:base="https://utf7.github.io/2022/06/12/word-specification/"><![CDATA[<p>书写用词规范,特别需要注意大小组合，否则会显得不专业：比如 hbase,mysql,sla,hadoop,clickhouse 都是不推荐的，常见的名词需要注意大小写</p>

<p>常见的名词规范如下：</p>

<p>Hadoop<br />
HDFS<br />
YARN<br />
MapReduce 或者 MR<br />
EMR<br />
Hive<br />
HBase<br />
Cassandra<br />
MySQL<br />
Spark<br />
Flink<br />
Kafka<br />
TensorFlow 或者 TF<br />
Elasticsearch 或者 ES<br />
Iceberg<br />
Paimon<br />
Hudi
Parquet<br />
ORC<br />
Presto<br />
Trino<br />
ClickHouse 或者 CK<br />
Doris<br />
StarRocks<br />
Milvus<br />
Zookeeper 或者 ZK<br />
RocksDB<br />
Java<br />
Linux <br />
CentOS<br />
Docker<br />
Kubernetes  或者 K8s<br />
SLA<br />
Tableau<br />
Agent<br />
LLM<br />
Harness</p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="文档" /><summary type="html"><![CDATA[程序员书写用词规范]]></summary></entry><entry><title type="html">将github 代码push 到自己的仓库并保留commit log</title><link href="https://utf7.github.io/2022/03/29/push-remote-repo-to-your-git/" rel="alternate" type="text/html" title="将github 代码push 到自己的仓库并保留commit log" /><published>2022-03-29T00:00:00+08:00</published><updated>2022-03-29T00:00:00+08:00</updated><id>https://utf7.github.io/2022/03/29/push-remote-repo-to-your-git</id><content type="html" xml:base="https://utf7.github.io/2022/03/29/push-remote-repo-to-your-git/"><![CDATA[<h2 id="1将github-某个版本放到自己的仓库保留commit-log">1.将github 某个版本放到自己的仓库，保留commit log</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:apache/hbase.git
cd hbase/
git tag
git checkout rel/2.4.11
git remote set-url origin ssh://xxx@gitlab.xx.com:port/bigdata/HBase
git remote -v
git branch test
git checkout test
git push origin test:2.4.11

</code></pre></div></div>]]></content><author><name>utf7 / 陈叶超</name></author><category term="git" /><summary type="html"><![CDATA[将github 代码push 到自己的仓库并保留commit log]]></summary></entry><entry><title type="html">给容器镜像瘦身的一个小技巧</title><link href="https://utf7.github.io/2022/03/14/reduce-docker-image-size/" rel="alternate" type="text/html" title="给容器镜像瘦身的一个小技巧" /><published>2022-03-14T00:00:00+08:00</published><updated>2022-03-14T00:00:00+08:00</updated><id>https://utf7.github.io/2022/03/14/reduce-docker-image-size</id><content type="html" xml:base="https://utf7.github.io/2022/03/14/reduce-docker-image-size/"><![CDATA[<h2 id="1镜像瘦身">1.镜像瘦身</h2>

<p>执行完 <code class="language-plaintext highlighter-rouge">yum</code> 命令以后，可以删除 <code class="language-plaintext highlighter-rouge">cache</code></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> yum -q clean all &amp;&amp; \
 rm -rf /var/cache/yum &amp;&amp; \
</code></pre></div></div>

<p>如:</p>

<p>https://github.com/utf7/Dockerfile/edit/master/jdk/jdk11/Dockerfile</p>

<p>这个镜像清理yum cache 可以减少 130多M</p>

<p><img width="998" alt="镜像大小的比较" src="/images/posts/k8s/dockerfile/clear-yum.jpg" /></p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="Docker" /><summary type="html"><![CDATA[给容器镜像瘦身的一个小tip]]></summary></entry><entry><title type="html">Spark Shuffle Service 配置不合理导致的任务失败以及NodeManager OOM 问题分析</title><link href="https://utf7.github.io/2021/05/12/spark-shuffle-service-problems/" rel="alternate" type="text/html" title="Spark Shuffle Service 配置不合理导致的任务失败以及NodeManager OOM 问题分析" /><published>2021-05-12T00:00:00+08:00</published><updated>2021-05-12T00:00:00+08:00</updated><id>https://utf7.github.io/2021/05/12/spark-shuffle-service-problems</id><content type="html" xml:base="https://utf7.github.io/2021/05/12/spark-shuffle-service-problems/"><![CDATA[<h2 id="1">1.</h2>

<p>最近集群有 Spark 任务会出现失败，查看 Spark 日志发现会有 Executor 挂掉的信息。</p>

<p>一般 Executor 挂掉话，通常会是几种情况：</p>

<p>1、Executor 自己挂了，比如申请内存不够等，executor 运行过程中 OOM等。</p>

<p>2、Executor 所在的节点出现问题，比如宕机 。</p>

<p>3、Executor 所在节点的NodeManager 挂了。因为Executor 都是NodeManager 进程 fork 出来的，NodeManager 挂的话，Executor 通常也会挂掉</p>

<p>4、操作系统内存紧张触发了OS的OOM Killer 功能，被操作系统干掉了。</p>

<p>5、Executor 被NodeManager watch 到内存超过使用的界限等原因，被NodeManager 给干掉了。</p>

<p>6、人为 kill （可能性较小）</p>

<h2 id="2">2.</h2>

<p>查看 Spark 日志并没有发现 Executor 有 OOM 的情况。</p>

<p>登陆 Executor 所在的节点，发现机器也正常。查看 NodeManager 情况</p>

<p>ps -ef|grep NodeManager|grep -v grep 
查看发现 NodeManager 也在。后来仔细看了一下 NodeManager 的启动时间发现不对，是最近的时间，证明 NodeManager 之前挂过，现在又启动了</p>

<p>（我们有 NodeManager 自动拉起的功能，所以挂掉以后立刻被拉起来了）</p>

<p>check NodeManager 日志</p>

<p>发现是 NodeManager OOM了</p>

<pre><code class="language-log">
WARN org.spark_project.io.netty.channel.AbstractChannelHandlerContext: An exception 'java.lang.OutOfMemoryError: Java heap space' [enable DEBUG level for full stacktrace] was thrown by a user handler's exceptionCaught() method while handling the following exception:
java.lang.OutOfMemoryError: GC overhead limit exceeded
 WARN org.spark_project.io.netty.channel.AbstractChannelHandlerContext: An exception '
java.lang.OutOfMemoryError: Java heap space
FATAL org.apache.hadoop.yarn.YarnUncaughtExceptionHandler: Thread Thread[DeletionService #3,5,main] threw an Error.  Shutting down now...
java.lang.OutOfMemoryError: Java heap space

</code></pre>

<p>OK，那么到这里原因大概就知道了</p>

<p>NodeManager OOM 导致的 Executor 挂掉。</p>

<p>那么为什么会 OOM 呢？</p>

<p>ps -ef|grep NodeManager|grep -v grep 
check 了一下 NodeManager 进程，发现设置了4096的堆内存（-Xmx4096m)</p>

<p>根据之前的经验线上应用 NodeManager 推荐大概内存是 8-12G 比较靠谱，4G 确实有点低了。</p>

<p>是不是就设置一下8-12G内存就 ok了呢？当然我们还是要分析一下为什么会OOM，到底什么占用了内存？是不是有内存泄露？是个例还是很多类似的？</p>

<p>使用 salt  全量统计了一下所有NodeManager日志关键字段，发现并不是个例有其他节点也存在 NodeManager OOM 的情况，找到另外一个节点来check一下（该节点其实GC不过来，已经卡死了）。</p>

<h2 id="3">3.</h2>

<p>首先是保留一些信息：</p>

<p>1、保留堆栈信息：</p>

<p>jstack 31549 &gt;nm-31459.jstack1;</p>

<p>sleep 2s;</p>

<p>jstack 31549 &gt;nm-31459.jstack2;</p>

<p>sleep 5s;</p>

<p>jstack 31549 &gt; nm-31459.jstack3;</p>

<p>2、保留句柄</p>

<p>lsof -p  31549 &gt;  nm-31459.fd</p>

<p>3、check 一下GC</p>

<p>jstat -gcutil 分析的gc 情况.</p>

<p>图片</p>

<p>4、jmap 看一下内存情况：</p>

<p>jmap -histo 31549 &gt;jmap-nm-31549.jmap</p>

<p>5、dump 内存</p>

<p>jmap -dump:format=b,file=nm-31549.heap.bin 31549</p>

<p>check 下 jmap 的信息</p>

<p>图片</p>

<p>发现有大量的LocalCache 对象以及ShuffleIndexInformation</p>

<p>ps：</p>

<p>[B 是指byte[] 数组</p>

<p>[C 是char[] 数组</p>

<p>一般 Java 进程 jmap 大部分都是 byte[],char[],String 这些，是比较正常的通常看不出来什么，基本上没啥关系。</p>

<p>主要怀疑对象在 ShuffleIndexInformation 和 LocalCache 上面</p>

<p>check 了一下代码，发现 Hadoop 中并没有这个代码。</p>

<p>从名字看这块肯定是 Shuffle 相关的，所以自然而然就想到了Spark Shuffle Service 。这里简单说一下，通常 Shuffle 是在 Executor 中的，executor 挂的话，就会导致shuffle 数据丢失失败，所以spark 后来做了 Shuffle Service，原理是将 Shuffle 放到 NodeManager 中来做，NodeManager 一般不忙，用来做 Shuffle 也是可以的，这样的话，Executor 挂了，NodeManager 其实还在的，有一定的稳定性提高，当然现在还有一个 Shuffle 思路是将 shuffle 做成单独的服务放在外面，这是另外的一个话题了。</p>

<p>查看 Spark 代码，Spark Shuffle Service 的代码在 NodeManager 中跑，ExternalShuffleBlockResolver 内部有一个</p>

<p>LoadingCache&lt;File, ShuffleIndexInformation&gt; shuffleIndexCache
该 cache 主要是用于 cache shuffle 的 index 信息。</p>

<p>cache 默认配置的是100m，由参数spark.shuffle.service.index.cache.size来配置。</p>

<p>查看当前配置发现是4096m</p>

<p>grep  -A 1 “spark.shuffle.service.index.cache.size” /etc/apps/hadoop-conf/yarn-site.xml</p>
<name>spark.shuffle.service.index.cache.size</name>
<value>4096m</value>
<p>当前 NodeManager 配置也就 4096m，所以当 cache 到一定程度的时候，oom 就可想而知了。</p>

<p>cache 相关代码设置如下：</p>

<p>ExternalShuffleBlockResolver(
      TransportConf conf,
      File registeredExecutorFile,
      Executor directoryCleaner) throws IOException {
    this.conf = conf;
    this.registeredExecutorFile = registeredExecutorFile;
    String indexCacheSize = conf.get(“spark.shuffle.service.index.cache.size”, “100m”);
    CacheLoader&lt;File, ShuffleIndexInformation&gt; indexCacheLoader =
        new CacheLoader&lt;File, ShuffleIndexInformation&gt;() {
          public ShuffleIndexInformation load(File file) throws IOException {
            return new ShuffleIndexInformation(file);
          }
        };
    shuffleIndexCache = CacheBuilder.newBuilder()
      .maximumWeight(JavaUtils.byteStringAsBytes(indexCacheSize))
      .weigher(new Weigher&lt;File, ShuffleIndexInformation&gt;() {
        public int weigh(File file, ShuffleIndexInformation indexInfo) {
          return indexInfo.getSize();//这块计算其实是有点问题的
        }
      })
      .build(indexCacheLoader);</p>

<p>对 dump 出来的内存进行分析发现也是与上面的结论是一致的：</p>

<p>发现4G内存，shuffleIndexCache 就占用了3.7G</p>

<p>图片</p>

<p>图片</p>

<p>check 了代码其实感觉代码还是有一些改进的地方的：</p>

<p>具体见注释</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shuffleIndexCache = CacheBuilder.newBuilder()
  .maximumWeight(JavaUtils.byteStringAsBytes(indexCacheSize))
  .weigher(new Weigher&lt;File, ShuffleIndexInformation&gt;() {
    public int weigh(File file, ShuffleIndexInformation indexInfo) {
      //return indexInfo.getSize();
      //这块计算其实是有点问题的，只计算了indexInfo文件size ，其实有会有ovehead的
      //当然这个问题主要看应该不是overhead，而是堆内存一共就配置了4096m，而cache 也配置了这么大。
      //实际上cache 是会超过配置的值的，比如文件多，但是index文件内容比较少的情况下。
      //这块代码做了一点点稍微小小的改动，具体其实是可以参考HBase d等代码来实际计算java对象大小。
      // 不过这块没有太大必要这么细，但是还是需要考虑overhead
      //当然对于index.cache.size 设置成256m的话，内存8G的NM的话，即使有8倍的overhead，其实问题也不大。
      //我们这里设置成了512m
       return file.getAbsolutePath().length() + 128+file indexInfo.getSize();
      

    }
  }) 最终做了如下三个修改：
</code></pre></div></div>

<p>1.修改 spark.shuffle.service.index.cache.size=512m</p>

<p>2.同时修改NodeManager 内存为10240m（10G）</p>

<p>3.同时对shuffleIndexCache内存占用的计算做了一些小小的修正（这里不是主要原因，这次主要还是内存配置于cache.size不匹配导致的，但是内存配置即使比较大的情况下，如果shuffleIndexCache配置也比较大，也有可能会出现可能shuffleIndexCache配置2g，实际上shuffleIndexCache占用远大于2g的情况会出现问题，所以规避一下风险。</p>

<p>修改以后任务稳定了很多。</p>

<p>观察了一段时间，发现再也没有 NodeManager OOM了</p>

<p>后来搜了一下社区，确实有类似的关于 shuffleIndexCache 内存计算的改进的改进。相关jira：</p>

<p>https://issues.apache.org/jira/browse/SPARK-21501</p>

<p>https://issues.apache.org/jira/browse/SPARK-33206</p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="Spark" /><summary type="html"><![CDATA[spark.shuffle.service.index.cache.size 参数配置不合理问题]]></summary></entry><entry><title type="html">salt 中执行 awk 注意事项</title><link href="https://utf7.github.io/2020/11/10/salt-exec-awk-failed-case/" rel="alternate" type="text/html" title="salt 中执行 awk 注意事项" /><published>2020-11-10T00:00:00+08:00</published><updated>2020-11-10T00:00:00+08:00</updated><id>https://utf7.github.io/2020/11/10/salt-exec-awk-failed-case</id><content type="html" xml:base="https://utf7.github.io/2020/11/10/salt-exec-awk-failed-case/"><![CDATA[<p>使用 <code class="language-plaintext highlighter-rouge">salt</code> 执行 awk 时，发现会执行失败</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt "*" cmd.run " ps -ef|grep NodeManager|grep -v grep|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk '{print $2}'|xargs lsof -p|grep REG|grep /usr/lib/locale/locale-archive"

</code></pre></div></div>

<p>会发现执行失败
$2 前面需要加转义符号</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt "*" cmd.run " ps -ef|grep NodeManager|grep -v grep|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk '{print \$2}'|xargs lsof -p|grep REG|grep /usr/lib/locale/locale-archive"

</code></pre></div></div>]]></content><author><name>utf7 / 陈叶超</name></author><category term="Salt" /><summary type="html"><![CDATA[salt 中执行 awk 注意事项]]></summary></entry><entry><title type="html">salt 中关于locale的问题</title><link href="https://utf7.github.io/2020/11/10/salt-locale-problem/" rel="alternate" type="text/html" title="salt 中关于locale的问题" /><published>2020-11-10T00:00:00+08:00</published><updated>2020-11-10T00:00:00+08:00</updated><id>https://utf7.github.io/2020/11/10/salt-locale-problem</id><content type="html" xml:base="https://utf7.github.io/2020/11/10/salt-locale-problem/"><![CDATA[<h2 id="记录一次诡异的-hivespark-乱码问题">记录一次诡异的 Hive/Spark 乱码问题</h2>

<p>最近遇到一个诡异的事情，Hive/Spark 写入汉字乱码。</p>

<p>检查 Hive/Spark 的各种参数也没有发现有什么变动，客户端配置也是一样的。</p>

<p>有一个诡异的地方是</p>

<p><code class="language-plaintext highlighter-rouge">spark-sql -f xxx.sql,xxx.sql</code> 如果 <code class="language-plaintext highlighter-rouge">xxx.sql</code> 中如果有汉字，则也会有问题。</p>

<p><code class="language-plaintext highlighter-rouge">spark-submit client</code> 模式则没有问题，<code class="language-plaintext highlighter-rouge">cluster</code> 模式则会有问题。</p>

<p>这里面唯一区别是 <code class="language-plaintext highlighter-rouge">client</code> 的 <code class="language-plaintext highlighter-rouge">Driver</code> 在 本地，<code class="language-plaintext highlighter-rouge">cluster</code> 在 <code class="language-plaintext highlighter-rouge">YARN（NodeManager）</code> 节点。</p>

<p>另外 <code class="language-plaintext highlighter-rouge">Spark/MR</code> 读写数据也有问题，不过 <code class="language-plaintext highlighter-rouge">hive cli</code> 不启动 <code class="language-plaintext highlighter-rouge">MR</code> 的话，则没有问题。。。比如 <code class="language-plaintext highlighter-rouge">select * from t limit 10</code>  这种不会启动 <code class="language-plaintext highlighter-rouge">MR</code>，则是本地运行的。</p>

<p>分析来看，不管是 <code class="language-plaintext highlighter-rouge">Hive/Spark</code> 只要任务跑在 YARN 上面则会有问题。</p>

<p>所以这块感觉和 <code class="language-plaintext highlighter-rouge">NodeManager</code> 有关系，因为 <code class="language-plaintext highlighter-rouge">on YARN</code> 的进程都是 <code class="language-plaintext highlighter-rouge">NodeManager fork</code> 出来的。</p>

<p>因为最近在做 <code class="language-plaintext highlighter-rouge">Zstandard</code> 修改了代码，所以同事怀疑是否代码引起的。我反复check了代码，并没有涉及到字符编码相关的内容。不应该出现此类问题。</p>

<p>由于修改了 <code class="language-plaintext highlighter-rouge">Hadoop</code> 代码，需要重启 <code class="language-plaintext highlighter-rouge">NodeManager</code> ，最近变动就是重启了 <code class="language-plaintext highlighter-rouge">NodeManager</code>以及添加了 <code class="language-plaintext highlighter-rouge">Zstandard</code> 的支持。</p>

<p>登陆出现问题的主机</p>

<p>执行 <code class="language-plaintext highlighter-rouge">locale</code></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="nv">LANG</span><span class="o">=</span>en_US.UTF-8
    <span class="nv">LCCTYPE</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_NUMERIC</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_TIME</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_COLLATE</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_MONETARY</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_MESSAGES</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_PAPER</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_NAME</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_ADDRESS</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_TELEPHONE</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_MEASUREMENT</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_IDENTIFICATION</span><span class="o">=</span><span class="s2">"en_US.UTF-8"</span>
    <span class="nv">LC_ALL</span><span class="o">=</span>
</code></pre></div></div>

<p>貌似也没啥问题。百思不得其解：</p>

<p>此时赶快将节点回滚到之前的版本，然后慢慢重启 <code class="language-plaintext highlighter-rouge">NodeManager</code>，发现确实问题渐渐修复了。</p>

<p>还有另外一个现象是并不是所有内容都是乱码的，这个猜测和 <code class="language-plaintext highlighter-rouge">Map/Reduce</code> 以及 <code class="language-plaintext highlighter-rouge">Spark Executor</code> 跑在不同的 <code class="language-plaintext highlighter-rouge">NodeManager</code> 节点有关系。</p>

<p>这时候根据多年的经验直觉，赶快保存的异常的节点 <code class="language-plaintext highlighter-rouge">NodeManager</code> 保留的 <code class="language-plaintext highlighter-rouge">lsof</code> 句柄信息，</p>

<p>diff 一下进程加载的 fd，是不是少加载了什么导致的，比较2个进程加载的fd 发现有一个</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps <span class="nt">-ef</span>|grep <span class="nt">-v</span> <span class="nb">grep</span>|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk <span class="s1">'{print $2}'</span>|xargs lsof <span class="nt">-p</span> |sort <span class="nt">-n</span> <span class="o">&gt;</span> lsof_before   
</code></pre></div></div>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps <span class="nt">-ef</span>|grep <span class="nt">-v</span> <span class="nb">grep</span>|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk <span class="s1">'{print $2}'</span>|xargs lsof <span class="nt">-p</span> |sort <span class="nt">-n</span>  <span class="o">&gt;</span> lsof_after. 
</code></pre></div></div>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>diff lsof_before lsof_after
</code></pre></div></div>

<p>然后发现少了</p>

<p><code class="language-plaintext highlighter-rouge">/usr/lib/locale/locale-archive</code></p>

<p>咦，什么鬼。。。为啥没有这个，分明我登陆节点执行，<code class="language-plaintext highlighter-rouge">locale</code> 输出是正常的。</p>

<p>所以就把问题定位点放在 <code class="language-plaintext highlighter-rouge">/usr/lib/locale/locale-archive</code> 为什么少了这个地方。</p>

<p>出问题的 NodeManager 启动命令如下：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt <span class="s1">'nm-*'</span> cmd.run <span class="s1">'sudo -u yarn bash -c "source /etc/profile;yarn-daemon.sh start nodemanager"'</span>
</code></pre></div></div>

<p>我在一个节点测试了一下，使用</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo</span> <span class="nt">-u</span> yarn bash <span class="nt">-c</span> <span class="s2">"source /etc/profile;yarn-daemon.sh start nodemanager"</span>
</code></pre></div></div>

<p>启动一个 NM 测试，发现并没有什么异常。。。。太诡异了。。。</p>

<p>节点还没有重启完，我赶快把所有的节点检查了一下，是否都丢失 <code class="language-plaintext highlighter-rouge">/usr/lib/locale/locale-archive</code></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt <span class="s2">"*"</span> cmd.run <span class="s2">" ps -ef|grep -v grep|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk '{print </span><span class="se">\$</span><span class="s2">2}'|xargs lsof -p|grep REG|grep /usr/lib/locale/locale-archive"</span>

</code></pre></div></div>

<p>发现我之前启动的节点都是没有 <code class="language-plaintext highlighter-rouge">locale-archive</code> ，而同事新启动的则是没有问题的。莫不是启动方式有问题？？？诡异。<br />
执行： <code class="language-plaintext highlighter-rouge">salt -E "nm-*" cmd.run "locale"</code></p>

<pre><code class="language-LANG=en_US.UTF-8">    LC_CTYPE=C
    LC_NUMERIC=C
    LC_TIME=C
    LC_COLLATE=C
    LC_MONETARY=C
    LC_MESSAGES=C
    LC_PAPER=C
    LC_NAME=C
    LC_ADDRESS=C
    LC_TELEPHONE=C
    LC_MEASUREMENT=C
    LC_IDENTIFICATION=C
    LC_ALL=
</code></pre>
<p>发现 <code class="language-plaintext highlighter-rouge">locale</code> 很多输出都是<code class="language-plaintext highlighter-rouge">C</code>，这个与我之前在机器上面执行的结果是不一致的。</p>

<p>所以应该是 <code class="language-plaintext highlighter-rouge">salt</code> 导致的，问了一下同事是如何重启的，他是kill 掉 <code class="language-plaintext highlighter-rouge">NodeManager</code>,并没有启动，而是让监控脚本自动拉起的。</p>

<p>而我当时为了启动快一些，是直接使用 <code class="language-plaintext highlighter-rouge">salt</code> 批量 <code class="language-plaintext highlighter-rouge">stop</code> 然后 <code class="language-plaintext highlighter-rouge">start</code> 的。</p>

<p>至此原因排查出来的，原来是 <code class="language-plaintext highlighter-rouge">salt</code> 的锅。</p>

<p>后来去github 把salt 代码拉了下来，看了一下。问题出在这边：</p>

<p><code class="language-plaintext highlighter-rouge">salt</code> 默认会 <code class="language-plaintext highlighter-rouge">reset_system_locale</code>,代码截图如下：</p>

<p><img src="/images/posts/salt/reset_system_locale_code1.png" alt="reset_system_locale_code1" /></p>

<p><img src="/images/posts/salt/reset_system_locale_code2.png" alt="reset_system_locale_code1" /></p>

<p>个人感觉这边salt处理的很有问题，虽然提供了参数，但我仍然认为是个bug，不知道为啥会考虑默认重置locale。</p>

<p>很坑！！！</p>

<p>看了一下代码，可以执行<code class="language-plaintext highlighter-rouge">salt</code> 命令的时候，添加参数 <code class="language-plaintext highlighter-rouge">reset_system_locale=False</code> 解决：</p>

<p>修改 <code class="language-plaintext highlighter-rouge">NodeManger</code> 启动脚本,<code class="language-plaintext highlighter-rouge">Hive/Spark</code> 乱码问题解决</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt <span class="s1">'nm-*'</span> cmd.run <span class="nv">reset_system_locale</span><span class="o">=</span>False <span class="s1">'sudo -u yarn bash -c "source /etc/profile;yarn-daemon.sh start nodemanager"'</span>
</code></pre></div></div>

<p>验证：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt <span class="s2">"nm-*"</span> cmd.run <span class="s2">" ps -ef|grep -v grep|grep org.apache.hadoop.yarn.server.nodemanager.NodeManager|awk '{print </span><span class="se">\$</span><span class="s2">2}'|xargs lsof -p|grep REG|grep /usr/lib/locale/locale-archive"</span>
</code></pre></div></div>

<p>如果有使用 <code class="language-plaintext highlighter-rouge">salt</code> 的同学，具体可以使用如下命令来测试 测试这个问题：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>salt <span class="s1">'nm-*'</span> cmd.run <span class="nv">reset_system_locale</span><span class="o">=</span>False <span class="s1">'locale'</span>
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> salt 'nm-*' cmd.run  'locale'
</code></pre></div></div>

<p>PS：
关于 <code class="language-plaintext highlighter-rouge">locale</code> ： 可参考如下几个链接：</p>

<p>1.https://wiki.archlinux.org/index.php/locale<br />
2.https://man7.org/linux/man-pages/man1/localedef.1.html<br />
3.https://linuxhint.com/locales_debian/</p>]]></content><author><name>utf7 / 陈叶超</name></author><category term="Salt" /><summary type="html"><![CDATA[salt 中关于locale的问题]]></summary></entry><entry><title type="html">Spark SQL 正确的传递 Hive 参数</title><link href="https://utf7.github.io/2020/10/18/spark-sql-params/" rel="alternate" type="text/html" title="Spark SQL 正确的传递 Hive 参数" /><published>2020-10-18T00:00:00+08:00</published><updated>2020-10-18T00:00:00+08:00</updated><id>https://utf7.github.io/2020/10/18/spark-sql-params</id><content type="html" xml:base="https://utf7.github.io/2020/10/18/spark-sql-params/"><![CDATA[<p>使用 <code class="language-plaintext highlighter-rouge">spark-sql</code> 导入动态分区时，出现来错误</p>
<pre><code class="language-log">Error in query: org.apache.hadoop.hive.ql.metadata.HiveException: Number of dynamic partitions created is 1793, which is more than 1000. To solve this try to set hive.exec.max.dynamic.partitions to at least 1793.;
</code></pre>
<p>提示增加 <code class="language-plaintext highlighter-rouge">hive.exec.max.dynamic.partitions</code> 的值，
可是我明明通过 <code class="language-plaintext highlighter-rouge">--conf  hive.exec.max.dynamic.partitions=1000000</code> 为什么还是报错了？难道是参数没有传递进去？？？
执行 <code class="language-plaintext highlighter-rouge">spark-sql --help</code> ，通过查看 <code class="language-plaintext highlighter-rouge">spark-sql</code> 的帮助</p>

<p>原来 hive的参数是通过 <code class="language-plaintext highlighter-rouge">--hiveconf</code> 来传递的而不是 <code class="language-plaintext highlighter-rouge">--conf</code></p>

<p>使用 <code class="language-plaintext highlighter-rouge">--conf</code> 来传递 <code class="language-plaintext highlighter-rouge">spark</code> 参数
使用 <code class="language-plaintext highlighter-rouge">--hiveconf</code> 来传递 <code class="language-plaintext highlighter-rouge">hive</code> 参数
修改后为：</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">nohup </span>spark-sql  <span class="nt">--master</span> yarn  <span class="nt">--deploy-mode</span> client <span class="nt">--queue</span> root.test.myqueue  <span class="nt">--driver-memory</span> 24g   <span class="nt">--executor-cores</span> 2   <span class="nt">--executor-memory</span> 4g   <span class="nt">--num-executors</span> 256   <span class="nt">--conf</span> spark.driver.memoryOverhead<span class="o">=</span>1g   <span class="nt">--conf</span> spark.executor.memoryOverhead<span class="o">=</span>1g   <span class="nt">--conf</span> spark.speculation<span class="o">=</span><span class="nb">false</span> <span class="nt">--conf</span> spark.driver.maxResultSize<span class="o">=</span>12g <span class="nt">--conf</span> spark.sql.hive.filesourcePartitionFileCacheSize<span class="o">=</span>4621440000 <span class="nt">--conf</span> spark.sql.files.maxPartitionBytes<span class="o">=</span>268435456 <span class="nt">--conf</span> spark.sql.files.openCostInBytes<span class="o">=</span>0 <span class="nt">--conf</span> spark.sql.shuffle.partitions<span class="o">=</span>512 <span class="nt">--hiveconf</span> hive.exec.max.dynamic.partitions<span class="o">=</span>1000000 <span class="nt">--hiveconf</span> hive.exec.max.dynamic.partitions.pernode<span class="o">=</span>100000 <span class="nt">--hiveconf</span> hive.exec.max.created.files<span class="o">=</span>1000000 <span class="nt">-S</span> <span class="nt">-e</span> <span class="s2">"insert overwrite table tt partition(day,hour) select * from table1 where day&lt;='2020-10-11' distribute by day,hour , cast( floor(rand() * 8) as int);"</span>  <span class="o">&gt;</span> merge.log 2&gt;&amp;1 &amp; 
</code></pre></div></div>]]></content><author><name>utf7 / 陈叶超</name></author><category term="Spark" /><summary type="html"><![CDATA[Spark SQL 传递 Hive 参数]]></summary></entry></feed>